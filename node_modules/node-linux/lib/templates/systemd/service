[Unit]
Description={{description}}

#ExecStart={{nodescript}} {{execpath}}
[Service]
ExecStart={{execpath}} {{nodescript}}
Restart=always
SyslogIdentifier={{label}}
User={{user}}
Group={{group}}
Environment={{env}}

[Install]
WantedBy=multi-user.target
